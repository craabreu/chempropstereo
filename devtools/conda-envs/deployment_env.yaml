name: build
channels:
  - conda-forge
  - craabreu
dependencies:
  # Base dependencies
  - python
  - pip
  - chemprop

  # Conda build
  - anaconda-client
  - conda-build
  - boa

  # PyPI build
  - pip:
    - build
    - twine
